---
phase: 12-lightrag-dual-level-keyword-extraction-unified-search-tool
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pam/agent/keyword_extractor.py
  - src/pam/common/config.py
  - src/pam/agent/tools.py
autonomous: true
requirements:
  - SMART-02

must_haves:
  truths:
    - "extract_query_keywords() calls Claude Haiku and returns QueryKeywords with high_level_keywords and low_level_keywords lists"
    - "Keyword extraction uses a ~50-token prompt with 3 few-shot examples adapted from LightRAG"
    - "SMART_SEARCH_ES_LIMIT and SMART_SEARCH_GRAPH_LIMIT are configurable via env vars with defaults of 5"
    - "SMART_SEARCH_TOOL definition exists in ALL_TOOLS list with correct input_schema"
  artifacts:
    - path: "src/pam/agent/keyword_extractor.py"
      provides: "extract_query_keywords() async function + QueryKeywords dataclass + KEYWORD_EXTRACTION_PROMPT"
      min_lines: 50
    - path: "src/pam/common/config.py"
      provides: "smart_search_es_limit and smart_search_graph_limit Settings fields"
      contains: "smart_search_es_limit"
    - path: "src/pam/agent/tools.py"
      provides: "SMART_SEARCH_TOOL definition added to ALL_TOOLS"
      contains: "smart_search"
  key_links:
    - from: "src/pam/agent/keyword_extractor.py"
      to: "anthropic.AsyncAnthropic"
      via: "client.messages.create() call"
      pattern: "client\\.messages\\.create"
    - from: "src/pam/agent/tools.py"
      to: "ALL_TOOLS"
      via: "SMART_SEARCH_TOOL appended to list"
      pattern: "SMART_SEARCH_TOOL"
---

<objective>
Create the keyword extraction module, config settings, and tool definition for the smart_search agent tool.

Purpose: Establishes the building blocks needed by Plan 02 to wire up the full smart_search handler. The keyword extractor is the core LightRAG-inspired innovation -- classifying queries into entity-level (low) and theme-level (high) keywords that route to appropriate search backends.

Output: `keyword_extractor.py` module with `extract_query_keywords()`, config env vars for result limits, and `SMART_SEARCH_TOOL` in the tools list.
</objective>

<execution_context>
@/Users/datnguyen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/datnguyen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-lightrag-dual-level-keyword-extraction-unified-search-tool/12-CONTEXT.md
@.planning/phases/12-lightrag-dual-level-keyword-extraction-unified-search-tool/12-RESEARCH.md
@src/pam/agent/tools.py
@src/pam/common/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create keyword extraction module and add config settings</name>
  <files>
    src/pam/agent/keyword_extractor.py
    src/pam/common/config.py
  </files>
  <action>
Create `src/pam/agent/keyword_extractor.py` with:

1. **`QueryKeywords` dataclass** with fields:
   - `high_level_keywords: list[str]` (themes, concepts, relationship types)
   - `low_level_keywords: list[str]` (entities, proper nouns, technical terms)

2. **`KEYWORD_EXTRACTION_PROMPT`** constant string (adapted from LightRAG):
   - Instruct the model to extract high_level_keywords and low_level_keywords
   - Output JSON only with exactly those two keys
   - Include 3 few-shot examples from the research document:
     - "What services depend on the authentication module?" -> service dependencies/system architecture + authentication module
     - "How has the deployment process changed since January?" -> process evolution/deployment changes + deployment process/January
     - "What is the conversion rate formula?" -> metric definition/business analytics + conversion rate/formula
   - Use double braces `{{` / `}}` for literal JSON braces in the f-string template
   - The `{query}` placeholder goes at the end

3. **`extract_query_keywords()` async function**:
   - Signature: `async def extract_query_keywords(client: AsyncAnthropic, query: str, model: str = "claude-3-5-haiku-20241022", timeout: float = 15.0) -> QueryKeywords`
   - Format the prompt with the query
   - Call `client.messages.create()` with `max_tokens=100` and the specified model and timeout
   - Parse the response text as JSON via `json.loads()`
   - Return `QueryKeywords` from the parsed data, using `.get()` with empty list defaults
   - On `json.JSONDecodeError` / `KeyError` / `IndexError`: log warning with `structlog`, then re-raise (per user decision: return error to agent on extraction failure)
   - On any other exception: log warning with exc_info=True, then re-raise

Add to `src/pam/common/config.py` Settings class:
- `smart_search_es_limit: int = 5` (env var: SMART_SEARCH_ES_LIMIT)
- `smart_search_graph_limit: int = 5` (env var: SMART_SEARCH_GRAPH_LIMIT)

Place these fields in a new comment section `# Smart Search` after the existing `# DuckDB` section and before `# Ingestion`.
  </action>
  <verify>
    <automated>cd /Users/datnguyen/Projects/AI-Projects/pam-context && python -c "from pam.agent.keyword_extractor import extract_query_keywords, QueryKeywords, KEYWORD_EXTRACTION_PROMPT; print('OK:', len(KEYWORD_EXTRACTION_PROMPT) > 100)" && python -c "from pam.common.config import Settings; s = Settings(); print('ES limit:', s.smart_search_es_limit, 'Graph limit:', s.smart_search_graph_limit); assert s.smart_search_es_limit == 5; assert s.smart_search_graph_limit == 5"</automated>
    <manual>Verify keyword_extractor.py has 3 few-shot examples and uses Haiku model by default</manual>
  </verify>
  <done>
    - `extract_query_keywords` importable with correct signature
    - `QueryKeywords` dataclass has both keyword list fields
    - `KEYWORD_EXTRACTION_PROMPT` contains 3 few-shot examples
    - Config has `smart_search_es_limit=5` and `smart_search_graph_limit=5` defaults
  </done>
</task>

<task type="auto">
  <name>Task 2: Add SMART_SEARCH_TOOL definition to tools.py</name>
  <files>src/pam/agent/tools.py</files>
  <action>
Add the `SMART_SEARCH_TOOL` definition to `src/pam/agent/tools.py`:

1. **Define SMART_SEARCH_TOOL** (type: `dict[str, Any]`) after `GET_ENTITY_HISTORY_TOOL`:
   - `name`: `"smart_search"`
   - `description`: Keep it concise per user decision (all three search tools described equally, no forced preference). Use: "Search both documents and the knowledge graph in one call. Extracts key concepts and entities from your query, then searches documents (text/semantic) and the knowledge graph (relationships) concurrently. Returns document results and graph results in separate sections."
   - `input_schema`: object with one required `query` property (type string, description: "Natural language query to search across documents and the knowledge graph.")

2. **Add to ALL_TOOLS list**: Append `SMART_SEARCH_TOOL` at the end of the `ALL_TOOLS` list (after `GET_ENTITY_HISTORY_TOOL`). This brings the agent from 7 to 8 tools.

Do NOT modify the system prompt in agent.py yet -- that is Plan 02's responsibility.
  </action>
  <verify>
    <automated>cd /Users/datnguyen/Projects/AI-Projects/pam-context && python -c "from pam.agent.tools import ALL_TOOLS, SMART_SEARCH_TOOL; assert len(ALL_TOOLS) == 8; assert SMART_SEARCH_TOOL['name'] == 'smart_search'; assert 'query' in SMART_SEARCH_TOOL['input_schema']['properties']; print('OK: 8 tools, smart_search defined')"</automated>
  </verify>
  <done>
    - `SMART_SEARCH_TOOL` is defined with name "smart_search" and query input
    - `ALL_TOOLS` contains 8 tools (7 existing + smart_search)
    - Tool description is concise (one clear sentence per tool, not a comparison essay)
  </done>
</task>

</tasks>

<verification>
1. `python -c "from pam.agent.keyword_extractor import extract_query_keywords, QueryKeywords"` succeeds
2. `python -c "from pam.common.config import Settings; s = Settings(); assert s.smart_search_es_limit == 5"` succeeds
3. `python -c "from pam.agent.tools import ALL_TOOLS; assert len(ALL_TOOLS) == 8; assert ALL_TOOLS[-1]['name'] == 'smart_search'"` succeeds
4. `cd /Users/datnguyen/Projects/AI-Projects/pam-context && python -m ruff check src/pam/agent/keyword_extractor.py src/pam/agent/tools.py src/pam/common/config.py` passes with no errors
</verification>

<success_criteria>
- keyword_extractor.py exists with extract_query_keywords, QueryKeywords, and KEYWORD_EXTRACTION_PROMPT
- Config has smart_search_es_limit and smart_search_graph_limit with default 5
- SMART_SEARCH_TOOL is in ALL_TOOLS (8 total tools)
- All 3 files pass ruff linting
</success_criteria>

<output>
After completion, create `.planning/phases/12-lightrag-dual-level-keyword-extraction-unified-search-tool/12-01-SUMMARY.md`
</output>
