---
phase: 11-graph-polish-tech-debt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pam/api/routes/ingest.py
  - src/pam/api/routes/admin.py
  - src/pam/api/routes/documents.py
  - .planning/phases/06-neo4j-graphiti-infrastructure/06-01-SUMMARY.md
  - .planning/phases/06-neo4j-graphiti-infrastructure/06-02-SUMMARY.md
  - .planning/phases/06-neo4j-graphiti-infrastructure/06-03-SUMMARY.md
  - .planning/phases/07-ingestion-pipeline-extension-diff-engine/07-01-SUMMARY.md
  - .planning/phases/07-ingestion-pipeline-extension-diff-engine/07-02-SUMMARY.md
  - .planning/phases/08-agent-graph-tool-rest-graph-endpoints/08-01-SUMMARY.md
  - .planning/phases/08-agent-graph-tool-rest-graph-endpoints/08-02-SUMMARY.md
  - .planning/phases/09-graph-explorer-ui/09-01-SUMMARY.md
  - .planning/phases/09-graph-explorer-ui/09-02-SUMMARY.md
  - .planning/phases/09-graph-explorer-ui/09-03-SUMMARY.md
  - .planning/phases/10-bitemporal-timestamp-fix/10-01-SUMMARY.md
autonomous: true
requirements:
  - VIZ-06

must_haves:
  truths:
    - "ruff check --select B904 reports zero violations across src/pam/"
    - "All 11 SUMMARY.md files have requirements_completed field with id+desc pairs format"
  artifacts:
    - path: "src/pam/api/routes/ingest.py"
      provides: "B904 fix with raise ... from err"
      contains: "from err"
    - path: "src/pam/api/routes/admin.py"
      provides: "B904 fix with raise ... from err"
      contains: "from err"
    - path: "src/pam/api/routes/documents.py"
      provides: "B904 fix with raise ... from err"
      contains: "from err"
    - path: ".planning/phases/06-neo4j-graphiti-infrastructure/06-01-SUMMARY.md"
      provides: "Structured requirements_completed frontmatter"
      contains: "requirements_completed"
  key_links: []
---

<objective>
Fix all ruff B904 lint violations and standardize SUMMARY.md frontmatter across all milestone phases.

Purpose: Resolve the remaining v2.0 tech debt items identified by the milestone audit — 3 ruff B904 violations (raise without `from err`) and 11 SUMMARY.md files missing the structured `requirements_completed` format. This closes the tech debt items so the v2.0 milestone can be archived cleanly.

Output: Zero B904 violations, all 11 SUMMARY.md files with structured requirements_completed frontmatter.
</objective>

<execution_context>
@/Users/datnguyen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/datnguyen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-graph-polish-tech-debt/11-RESEARCH.md
@.planning/phases/11-graph-polish-tech-debt/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all ruff B904 violations across the codebase</name>
  <files>src/pam/api/routes/ingest.py, src/pam/api/routes/admin.py, src/pam/api/routes/documents.py</files>
  <action>
    Fix all 3 ruff B904 violations. Each follows the identical pattern — add `as err` to the except clause and `from err` to the raise.

    1. **src/pam/api/routes/ingest.py line 120-121:**
       ```python
       # Before:
       except (ValueError, KeyError):
           raise HTTPException(status_code=400, detail="Invalid cursor")
       # After:
       except (ValueError, KeyError) as err:
           raise HTTPException(status_code=400, detail="Invalid cursor") from err
       ```

    2. **src/pam/api/routes/admin.py line 59-60:**
       ```python
       # Before:
       except (ValueError, KeyError):
           raise HTTPException(status_code=400, detail="Invalid cursor")
       # After:
       except (ValueError, KeyError) as err:
           raise HTTPException(status_code=400, detail="Invalid cursor") from err
       ```

    3. **src/pam/api/routes/documents.py line 65-66:**
       ```python
       # Before:
       except (ValueError, KeyError):
           raise HTTPException(status_code=400, detail="Invalid cursor")
       # After:
       except (ValueError, KeyError) as err:
           raise HTTPException(status_code=400, detail="Invalid cursor") from err
       ```

    After making all 3 changes, run `ruff check --select B904 src/pam/` to confirm zero violations.

    NOTE: The research identified only ingest.py:121, but ruff finds the same pattern in admin.py:60 and documents.py:66. Fix ALL three per Pitfall 5 in the research ("Run ruff check --select B904 across the codebase to find all instances").
  </action>
  <verify>
    <automated>cd /Users/datnguyen/Projects/AI-Projects/pam-context && ruff check --select B904 src/pam/</automated>
  </verify>
  <done>ruff check --select B904 reports zero violations across src/pam/. All three cursor-decoding except clauses use `raise ... from err`.</done>
</task>

<task type="auto">
  <name>Task 2: Standardize requirements_completed frontmatter in all 11 SUMMARY.md files</name>
  <files>
    .planning/phases/06-neo4j-graphiti-infrastructure/06-01-SUMMARY.md
    .planning/phases/06-neo4j-graphiti-infrastructure/06-02-SUMMARY.md
    .planning/phases/06-neo4j-graphiti-infrastructure/06-03-SUMMARY.md
    .planning/phases/07-ingestion-pipeline-extension-diff-engine/07-01-SUMMARY.md
    .planning/phases/07-ingestion-pipeline-extension-diff-engine/07-02-SUMMARY.md
    .planning/phases/08-agent-graph-tool-rest-graph-endpoints/08-01-SUMMARY.md
    .planning/phases/08-agent-graph-tool-rest-graph-endpoints/08-02-SUMMARY.md
    .planning/phases/09-graph-explorer-ui/09-01-SUMMARY.md
    .planning/phases/09-graph-explorer-ui/09-02-SUMMARY.md
    .planning/phases/09-graph-explorer-ui/09-03-SUMMARY.md
    .planning/phases/10-bitemporal-timestamp-fix/10-01-SUMMARY.md
  </files>
  <action>
    Replace the `requirements-completed: [...]` line in each SUMMARY.md frontmatter with the structured `requirements_completed` format (underscore, not hyphen; id+desc pairs per user decision).

    Use 2-space indentation for the YAML sequence items. Keep YAML frontmatter delimiters (`---`) unchanged. Do not modify anything outside the frontmatter block.

    Here is the exact replacement for each file:

    **06-01-SUMMARY.md** — replace `requirements-completed: [INFRA-01, INFRA-02, INFRA-05]` with:
    ```yaml
    requirements_completed:
      - id: INFRA-01
        desc: Neo4j running in Docker with health check
      - id: INFRA-02
        desc: graphiti-core installed with Anthropic LLM
      - id: INFRA-05
        desc: Entity type taxonomy as Pydantic models
    ```

    **06-02-SUMMARY.md** — replace `requirements-completed: [INFRA-03, INFRA-04]` with:
    ```yaml
    requirements_completed:
      - id: INFRA-03
        desc: GraphitiService singleton on app.state
      - id: INFRA-04
        desc: get_graph_service dependency in deps.py
    ```

    **06-03-SUMMARY.md** — replace `requirements-completed: [INFRA-06]` with:
    ```yaml
    requirements_completed:
      - id: INFRA-06
        desc: NVL React packages installed in web/
    ```

    **07-01-SUMMARY.md** — replace `requirements-completed: [EXTRACT-01, EXTRACT-02, EXTRACT-03, EXTRACT-06, DIFF-01, DIFF-02]` with:
    ```yaml
    requirements_completed:
      - id: EXTRACT-01
        desc: Pipeline calls add_episode per segment
      - id: EXTRACT-02
        desc: Bi-temporal timestamps from document modified_at
      - id: EXTRACT-03
        desc: graph_synced column via Alembic migration
      - id: EXTRACT-06
        desc: Orphan prevention via episode tombstoning
      - id: DIFF-01
        desc: Entity-level change detection on re-ingestion
      - id: DIFF-02
        desc: Superseded edges get t_invalid set
    ```

    **07-02-SUMMARY.md** — replace `requirements-completed: [EXTRACT-04, EXTRACT-05, DIFF-03]` with:
    ```yaml
    requirements_completed:
      - id: EXTRACT-04
        desc: Graph extraction as non-blocking background step
      - id: EXTRACT-05
        desc: sync-graph endpoint retries failed extractions
      - id: DIFF-03
        desc: Diff summaries in SyncLog.details JSON
    ```

    **08-01-SUMMARY.md** — replace `requirements-completed: [GRAPH-01, GRAPH-02, GRAPH-03, GRAPH-06]` with:
    ```yaml
    requirements_completed:
      - id: GRAPH-01
        desc: search_knowledge_graph agent tool
      - id: GRAPH-02
        desc: get_entity_history agent tool
      - id: GRAPH-03
        desc: Point-in-time graph query via reference_time
      - id: GRAPH-06
        desc: Tool result capped at 3000 chars / 20 nodes
    ```

    **08-02-SUMMARY.md** — replace `requirements-completed: [GRAPH-04, GRAPH-05, GRAPH-06]` with:
    ```yaml
    requirements_completed:
      - id: GRAPH-04
        desc: GET /graph/neighborhood endpoint
      - id: GRAPH-05
        desc: GET /graph/entities endpoint
      - id: GRAPH-06
        desc: Tool result capped at 3000 chars / 20 nodes
    ```

    **09-01-SUMMARY.md** — replace `requirements-completed: [VIZ-03, VIZ-04]` with:
    ```yaml
    requirements_completed:
      - id: VIZ-03
        desc: Temporal timeline for edge history
      - id: VIZ-04
        desc: Ingestion graph preview with diff overlay
    ```

    **09-02-SUMMARY.md** — replace `requirements-completed: [VIZ-01, VIZ-02, VIZ-03, VIZ-05, VIZ-06]` with:
    ```yaml
    requirements_completed:
      - id: VIZ-01
        desc: Graph explorer with NVL as collapsible panel
      - id: VIZ-02
        desc: Entity click expands detail panel
      - id: VIZ-03
        desc: Temporal timeline for edge history
      - id: VIZ-05
        desc: VITE_GRAPH_ENABLED feature flag
      - id: VIZ-06
        desc: Graph indexing in progress empty state
    ```

    **09-03-SUMMARY.md** — replace `requirements-completed: [VIZ-04]` with:
    ```yaml
    requirements_completed:
      - id: VIZ-04
        desc: Ingestion graph preview with diff overlay
    ```

    **10-01-SUMMARY.md** — replace `requirements-completed: [EXTRACT-02]` with:
    ```yaml
    requirements_completed:
      - id: EXTRACT-02
        desc: Bi-temporal timestamps from document modified_at
    ```

    After all replacements, verify that each file's YAML frontmatter parses correctly by checking the `---` delimiters are intact and indentation is consistent 2-space.
  </action>
  <verify>
    <automated>cd /Users/datnguyen/Projects/AI-Projects/pam-context && python -c "
import yaml, glob
files = glob.glob('.planning/phases/*/0*-SUMMARY.md') + glob.glob('.planning/phases/*/1*-SUMMARY.md')
errors = []
for f in sorted(files):
    with open(f) as fh:
        content = fh.read()
    parts = content.split('---', 2)
    if len(parts) < 3:
        errors.append(f'{f}: no frontmatter')
        continue
    try:
        fm = yaml.safe_load(parts[1])
    except Exception as e:
        errors.append(f'{f}: YAML parse error: {e}')
        continue
    rc = fm.get('requirements_completed')
    if rc is None:
        if fm.get('requirements-completed') is not None:
            errors.append(f'{f}: still uses old requirements-completed (hyphenated)')
        else:
            errors.append(f'{f}: missing requirements_completed field')
    elif isinstance(rc, list) and len(rc) > 0:
        if not isinstance(rc[0], dict) or 'id' not in rc[0]:
            errors.append(f'{f}: requirements_completed items not in id+desc format')
if errors:
    for e in errors:
        print(f'ERROR: {e}')
    exit(1)
else:
    print(f'All {len(files)} SUMMARY.md files have valid requirements_completed frontmatter')
"</automated>
  </verify>
  <done>All 11 SUMMARY.md files have requirements_completed field using id+desc pairs format with underscored key name. YAML frontmatter parses correctly in all files.</done>
</task>

</tasks>

<verification>
1. `ruff check --select B904 src/pam/` — zero violations
2. `ruff check src/pam/api/routes/ingest.py src/pam/api/routes/admin.py src/pam/api/routes/documents.py` — no errors
3. All 11 SUMMARY.md files parse valid YAML with structured requirements_completed
4. No SUMMARY.md file still uses the old `requirements-completed` (hyphenated) key
</verification>

<success_criteria>
- Zero ruff B904 violations across the entire src/pam/ tree (was 3)
- All 11 SUMMARY.md files use `requirements_completed:` (underscore) with `- id: X` / `desc: Y` pairs
- No old-format `requirements-completed: [...]` (hyphenated, bracket) entries remain
- All YAML frontmatter remains parseable
</success_criteria>

<output>
After completion, create `.planning/phases/11-graph-polish-tech-debt/11-02-SUMMARY.md`
</output>
