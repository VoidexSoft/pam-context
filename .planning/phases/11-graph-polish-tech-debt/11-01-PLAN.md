---
phase: 11-graph-polish-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pam/api/routes/graph.py
  - web/src/api/client.ts
  - web/src/hooks/useGraphExplorer.ts
  - web/src/pages/GraphExplorerPage.tsx
autonomous: true
requirements:
  - VIZ-06

must_haves:
  truths:
    - "Graph explorer shows 'No documents ingested' with link to ingest page when no documents exist in PG"
    - "Graph explorer shows 'Graph indexing in progress' with pending document count when documents exist but entityCount is 0"
    - "Graph data endpoints (neighborhood, entities, history) return 503 with {detail: 'Graph service unavailable'} when graph_service is None"
    - "graph_status returns 200 with status 'unavailable' and document counts when graph_service is None (not 503)"
  artifacts:
    - path: "src/pam/api/routes/graph.py"
      provides: "Extended graph_status with document_count/graph_synced_count + null guards on all endpoints"
      contains: "document_count"
    - path: "web/src/api/client.ts"
      provides: "Updated GraphStatus interface with document_count and graph_synced_count fields"
      contains: "document_count"
    - path: "web/src/hooks/useGraphExplorer.ts"
      provides: "Hook state for documentCount and graphSyncedCount extracted from graph status response"
      contains: "documentCount"
    - path: "web/src/pages/GraphExplorerPage.tsx"
      provides: "Two-branch empty state: no-documents vs indexing-in-progress"
      contains: "Graph indexing in progress"
  key_links:
    - from: "src/pam/api/routes/graph.py"
      to: "web/src/api/client.ts"
      via: "GraphStatus response shape with document_count + graph_synced_count"
      pattern: "document_count.*graph_synced_count"
    - from: "web/src/hooks/useGraphExplorer.ts"
      to: "web/src/pages/GraphExplorerPage.tsx"
      via: "documentCount and graphSyncedCount state passed from hook to page"
      pattern: "documentCount"
---

<objective>
Implement VIZ-06 empty state and graph service null guards.

Purpose: Complete the final v2.0 spec requirement (VIZ-06) by adding two distinct empty states to the graph explorer (no documents vs indexing in progress), and harden graph REST endpoints with explicit null guards returning 503 when graph_service is unavailable.

Output: Extended graph_status endpoint with PG document counts, two-branch frontend empty state, explicit 503 on three data endpoints when Neo4j is unavailable.
</objective>

<execution_context>
@/Users/datnguyen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/datnguyen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-graph-polish-tech-debt/11-RESEARCH.md
@.planning/phases/11-graph-polish-tech-debt/11-CONTEXT.md
@.planning/phases/09-graph-explorer-ui/09-02-SUMMARY.md
@src/pam/api/routes/graph.py
@web/src/api/client.ts
@web/src/hooks/useGraphExplorer.ts
@web/src/pages/GraphExplorerPage.tsx
@src/pam/api/deps.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend graph_status with PG document counts and add null guards to graph endpoints</name>
  <files>src/pam/api/routes/graph.py</files>
  <action>
    Modify the graph.py router to add explicit null guards and extend graph_status:

    1. **Add imports** at the top of graph.py:
       - `from sqlalchemy import select, func` (for PG count queries)
       - `from sqlalchemy.ext.asyncio import AsyncSession` (for db type hint)
       - `from pam.api.deps import get_db` (for DI)
       - `from pam.common.models import Document` (for the Document model)

    2. **Extend graph_status endpoint:**
       - Add `db: AsyncSession = Depends(get_db)` parameter to the function signature
       - At the TOP of the function (before any Neo4j queries), query PG for document counts:
         ```python
         total_doc_result = await db.execute(select(func.count()).select_from(Document))
         document_count = total_doc_result.scalar() or 0
         synced_result = await db.execute(
             select(func.count()).select_from(Document).where(Document.graph_synced == True)  # noqa: E712
         )
         graph_synced_count = synced_result.scalar() or 0
         ```
       - Add null guard for graph_service AFTER PG queries: if `graph_service is None`, return a 200 response with `"status": "unavailable"`, empty entity_counts, total_entities=0, last_sync_time=None, plus the new document_count and graph_synced_count fields. Do NOT raise 503 here — graph_status is a status endpoint that must always return data.
       - Add `"document_count": document_count` and `"graph_synced_count": graph_synced_count` to the existing success return dict (backward compatible — adding new fields only).

    3. **Add null guard to graph_neighborhood endpoint:**
       - At the very top of the handler (before any Neo4j operations), add:
         ```python
         if graph_service is None:
             raise HTTPException(status_code=503, detail="Graph service unavailable")
         ```

    4. **Add null guard to graph_entities endpoint** (same pattern as step 3).

    5. **Add null guard to entity_history endpoint** (same pattern as step 3).

    NOTE: Keep all existing try/except Exception blocks — the null guard is an ADDITIONAL early return, not a replacement. The existing try/except handles Neo4j connection failures at runtime; the null guard handles the case where the service was never created at startup.

    Follow the existing code style. Use `# noqa: E712` on the `== True` comparison for SQLAlchemy boolean column filtering (same pattern as documents.py /stats endpoint).
  </action>
  <verify>
    <automated>cd /Users/datnguyen/Projects/AI-Projects/pam-context && python -c "from pam.api.routes.graph import router; print('graph router imports OK')" && ruff check src/pam/api/routes/graph.py</automated>
    <manual>Review that graph_status returns document_count and graph_synced_count in both success and unavailable paths</manual>
  </verify>
  <done>graph_status returns document_count and graph_synced_count from PG in all paths (success, unavailable, Neo4j error). Three data endpoints (neighborhood, entities, history) raise HTTPException(503) when graph_service is None with detail "Graph service unavailable".</done>
</task>

<task type="auto">
  <name>Task 2: Update frontend types, hook state, and GraphExplorerPage empty states</name>
  <files>web/src/api/client.ts, web/src/hooks/useGraphExplorer.ts, web/src/pages/GraphExplorerPage.tsx</files>
  <action>
    1. **Update GraphStatus interface in client.ts:**
       - Add `document_count: number` and `graph_synced_count: number` to the `GraphStatus` interface.
       - These are new fields — do not remove or rename any existing fields.

    2. **Update useGraphExplorer hook to expose document counts:**
       - Add `documentCount` and `graphSyncedCount` state variables (default 0).
       - In the success path of the `getGraphStatus()` call (where entityCount is already extracted), also extract `document_count` and `graph_synced_count` from the response and set state.
       - Return `documentCount` and `graphSyncedCount` from the hook alongside existing return values.

    3. **Update GraphExplorerPage.tsx two-branch empty state:**
       - Destructure `documentCount` and `graphSyncedCount` from the `useGraphExplorer()` hook call.
       - Replace the current single empty state block (when entityCount === 0) with two branches:

       **Branch A — No documents exist (documentCount === 0):**
       - Centered layout with icon above text (use an appropriate SVG icon, e.g., a document/folder icon — Claude's discretion on exact icon)
       - Heading: "No documents ingested"
       - Subtext: "Ingest documents to build the knowledge graph"
       - A Link component (from react-router-dom) navigating to `/admin` styled as a button: "Go to Ingest"
       - Use the SAME color palette as the rest of the graph explorer — consistent styling, not muted/gray

       **Branch B — Documents exist but graph not synced (documentCount > 0 and entityCount === 0):**
       - Centered layout with icon above text (use a graph/sync icon — Claude's discretion)
       - Heading: "Graph indexing in progress"
       - Subtext showing count: e.g., "5 documents awaiting graph indexing" using `documentCount - graphSyncedCount` for the pending count (if graphSyncedCount is 0, this equals documentCount)
       - No action button — user just waits for sync
       - Same color palette as the explorer

       Both branches should feel like part of the graph explorer — centered in the canvas area, not a separate error page. Use Tailwind classes consistent with the existing component.

    4. **Handle graph_service unavailable state in frontend:**
       - When the `getGraphStatus()` response includes `status: "unavailable"`, the hook should still set documentCount and graphSyncedCount (so empty states render correctly).
       - If the API returns a 503 from data endpoints, the existing error handling in the hook should display the error message. No auto-retry — manual refresh only (per user decision).
  </action>
  <verify>
    <automated>cd /Users/datnguyen/Projects/AI-Projects/pam-context/web && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Review that two empty state branches render correctly with the right conditions</manual>
  </verify>
  <done>GraphStatus interface has document_count and graph_synced_count. useGraphExplorer exposes documentCount and graphSyncedCount. GraphExplorerPage shows "No documents ingested" with ingest link when documentCount===0, and "Graph indexing in progress" with pending count when documents exist but entityCount===0.</done>
</task>

</tasks>

<verification>
1. `ruff check src/pam/api/routes/graph.py` — no lint errors
2. `cd web && npx tsc --noEmit` — no TypeScript errors
3. Graph status endpoint returns document_count and graph_synced_count in response
4. Null guard on neighborhood/entities/history endpoints raises 503 when graph_service is None
5. graph_status returns 200 with "unavailable" status when graph_service is None (NOT 503)
6. Frontend shows two distinct empty states based on document count vs entity count
</verification>

<success_criteria>
- VIZ-06 requirement satisfied: "Graph indexing in progress" state shown when documents exist but graph_synced count is 0
- "No documents" empty state shown with link to ingest page when no documents exist
- All graph data endpoints return structured 503 when graph_service is None
- graph_status returns degraded 200 with PG counts even when Neo4j is unavailable
- TypeScript compiles without errors
- Python linter passes on graph.py
</success_criteria>

<output>
After completion, create `.planning/phases/11-graph-polish-tech-debt/11-01-SUMMARY.md`
</output>
